{
  "name": "projection",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/projection/src",
  "projectType": "library",
  "tags": ["type:lib", "scope:projection"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "bun run build",
        "cwd": "libs/projection"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "bun test",
        "cwd": "libs/projection"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "bunx tsc --noEmit",
        "cwd": "libs/projection"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "bunx tsc --noEmit",
        "cwd": "libs/projection"
      }
    }
  }
}
