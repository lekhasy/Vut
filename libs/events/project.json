{
  "name": "events",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/events/src",
  "projectType": "library",
  "tags": ["type:lib", "scope:events"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "bun run build",
        "cwd": "libs/events"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "bun test",
        "cwd": "libs/events"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "bunx tsc --noEmit",
        "cwd": "libs/events"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "bunx tsc --noEmit",
        "cwd": "libs/events"
      }
    }
  }
}
