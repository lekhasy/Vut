apiVersion: v1
kind: Service
metadata:
  name: vut-postgresql
  namespace: vut
  labels:
    app.kubernetes.io/part-of: vut
    app.kubernetes.io/component: database
    app.kubernetes.io/name: postgresql
spec:
  type: ClusterIP
  ports:
    - name: postgresql
      port: 5432
      targetPort: 5432
      protocol: TCP
  selector:
    app.kubernetes.io/part-of: vut
    app.kubernetes.io/component: database
    app.kubernetes.io/name: postgresql
