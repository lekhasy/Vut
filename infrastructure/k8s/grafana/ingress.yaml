apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: velucid-grafana-ingress
  namespace: velucid
  labels:
    app.kubernetes.io/part-of: velucid
    app.kubernetes.io/component: observability
    app.kubernetes.io/name: grafana
spec:
  ingressClassName: traefik
  rules:
    - host: grafana.velucid.app
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: velucid-grafana
                port:
                  number: 3000